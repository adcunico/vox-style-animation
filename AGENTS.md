# Agent Instructions

Read `CLAUDE.md` — it is the single source of truth for this project: commands,
project structure, the keyframe-pair workflow, and the safety-filter playbook
(no famous names in video prompts, painted portraits only, static faces).

For the full process, see `VOX-SEQUENTIAL-SCENE-WORKFLOW.md`. For the visual
rules, see `VOX-STYLE.md`. To run this workflow on a new topic with Claude Code,
use the `vox-style-video` skill in `.claude/skills/`.
